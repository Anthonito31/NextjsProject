import { Roboto, Bebas_Neue, Playfair_Display } from 'next/font/google';

export const roboto = Roboto({subsets: ['latin'], weight: ['100', '400', '700']});

export const bebas = Bebas_Neue({subsets: ['latin'], weight: '400'})

export const playfair = Playfair_Display({subsets: ['latin'], weight: '400'})
